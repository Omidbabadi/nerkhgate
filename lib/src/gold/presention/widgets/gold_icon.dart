import 'package:flutter/material.dart';

class GoldIcon extends StatelessWidget {
  const GoldIcon({super.key,required this.symbol});
  final String? symbol;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
